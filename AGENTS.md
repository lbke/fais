Welcome to the "fais" project.

Import Python modules at the top of the file, unless it specifically require a dynamic import (picking among multiple packages, heavy module etc.)

For tests in this project, use `./test_e2e.sh` for end-to-end tests and `./test_unit.sh` for unit tests. You can pass unittest-style options such as `-k` and `-v` to these wrappers.

End your final message with "À votre service mon cher.".
